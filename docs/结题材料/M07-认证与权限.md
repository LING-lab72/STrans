# M07：认证与权限 — AuthStore + auth_store

## 1. 模块定位

认证模块 (`backend/app/services/auth.py`，25 符号，118 行) 由三个子组件构成：

| 组件 | 文件 | 职责 |
|---|---|---|
| `AuthStore` | `auth.py` | 密码哈希、用户 CRUD、会话 Token、验证码、审计 |
| `auth_store` (单例) | `auth.py:117` | 全局认证服务实例 |
| `auth_store.py` | `auth_store.py` | 会话持久化 (SQLite)、用户管理、审计日志 |

## 2. 认证方案

### 2.1 密码安全

```python
def _hash_password(self, password, salt=None):
    salt = salt or secrets.token_hex(16)         # 16字节随机盐
    digest = hashlib.pbkdf2_hmac(
        "sha256",
        password.encode("utf-8"),
        salt.encode("utf-8"),
        120_000                                    # 12万次迭代
    )
    return salt, digest.hex()
```

**安全属性**：
- PBKDF2-SHA256，12 万次迭代 → 暴力破解计算成本高
- 16 字节随机盐 → 相同密码不同哈希
- 管理员初始密码可通过环境变量 `STRANS_ADMIN_PASSWORD` 覆盖

### 2.2 会话 Token

```python
def make_token(self, user, expires_in=8*3600):
    payload = {"username": user["username"], "role": user["role"],
               "exp": int(time.time()) + expires_in}
    body = base64_urlsafe_encode(json.dumps(payload))
    signature = hmac.new(SECRET, body, sha256).hexdigest()
    return f"{body}.{signature}"         # JWT-like 格式
```

**设计**：
- 自包含 Token（无需查数据库），Base64 + HMAC 签名
- 8 小时过期
- `hmac.compare_digest` 防时序攻击

**注意**：使用自签名 Token 而非标准 JWT，减少了依赖，但 `SECRET` 硬编码为 `"strans-local-demo-secret"` 仅适合演示环境。

### 2.3 图片验证码

```python
# (auth_store.py 中)
def new_captcha(self):
    code = random 6位字母数字
    captcha_id = uuid4
    _captchas[captcha_id] = (code, expires_at=now+300s)  # 5分钟有效
    # 返回 captcha_id + base64 图片
```

注册和登录均需验证码，防自动化攻击。

## 3. 用户管理

### 3.1 数据库 Schema

```sql
CREATE TABLE system_users (
    username TEXT PRIMARY KEY,
    password_hash TEXT NOT NULL,
    salt TEXT NOT NULL,
    role TEXT DEFAULT 'user',    -- 'admin' / 'user'
    enabled INTEGER DEFAULT 1,
    created_at TEXT NOT NULL
);

CREATE TABLE system_sessions (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    token TEXT UNIQUE,
    created_at TEXT,
    expires_at TEXT
);

CREATE TABLE audit_logs (
    id INTEGER PRIMARY KEY,
    username TEXT,
    action TEXT,                 -- 'create_camera' / 'delete_whitelist' / ...
    target TEXT,                 -- 操作对象标识
    detail TEXT,                 -- 详细描述
    created_at TEXT
);
```

### 3.2 RBAC 权限模型

| 角色 | 权限范围 |
|---|---|
| admin | 全部 API + 用户管理 + 摄像头管理 + 白名单修改 + 模型配置 |
| user | 查看监控、查询历史、查看告警、修改自己密码、查看白名单 |

### 3.3 API 权限注入 (main.py)

```python
def current_user(authorization=Header(None)):
    user = auth_store.get_user_by_token(token)
    if not user: raise HTTPException(401)
    return user

def current_admin(user=Depends(current_user)):
    if user["role"] != "admin": raise HTTPException(403)
    return user

# 使用:
@app.get("/api/admin/users")             # → Depends(current_admin)
@app.get("/api/history")                  # → Depends(current_user)
@app.get("/api/health")                   # → 无认证
```

## 4. 审计日志

所有管理操作均记录：

```python
auth_store.add_audit(username, action, target, detail)
```

审计覆盖的操作类型：
- `create_camera` / `update_camera` / `delete_camera`
- `upsert_whitelist` / `delete_whitelist`
- `update_user` / `delete_user` / `reset_password`
- `change_password`
- `configure_scheduler`
- `update_model_config`
- `delete_history` / `purge_history`
- `handle_incident` / `export_evidence`
- `generate_report`

## 5. API 接口

### 认证接口

| 端点 | 方法 | 权限 | 说明 |
|---|---|---|---|
| `/api/auth/captcha` | GET | 公开 | 获取验证码(ID+图片) |
| `/api/auth/register` | POST | 公开 | 注册(需验证码) |
| `/api/auth/login` | POST | 公开 | 登录(需验证码) → token |
| `/api/auth/me` | GET | user | 当前用户信息 |
| `/api/auth/logout` | POST | user | 登出(销毁token) |
| `/api/auth/password` | PUT | user | 修改自己密码 |

### 管理接口

| 端点 | 方法 | 权限 | 说明 |
|---|---|---|---|
| `/api/admin/users` | GET | admin | 用户列表 |
| `/api/admin/users/{id}` | PUT | admin | 修改角色/启停 |
| `/api/admin/users/{id}/password` | PUT | admin | 重置密码 |
| `/api/admin/users/{id}` | DELETE | admin | 删除用户 |
| `/api/admin/audit` | GET | admin | 审计日志 |

### 安全约束

- 不能禁用/删除自己的账号
- 修改密码后要求重新登录 (`reauth_required: true`)
- 视频源地址在审计中脱敏

## 6. 源码关键行号

| 功能 | 文件 | 行号 |
|---|---|---|
| AdminConfig | auth_store.py | ~10 |
| AuthStore.\_\_init\_\_ | auth.py | 22 |
| _init_db | auth.py | 31 |
| _hash_password | auth.py | 55 |
| create_user | auth.py | 60 |
| authenticate | auth.py | 80 |
| make_token | auth.py | 90 |
| verify_token | auth.py | 100 |
| current_user / current_admin | main.py | 174/181 |
